# Point this at your Pi4. Use its hostname (…​.local via mDNS) or IP.
# The ansible_user comes from config.yml; override per-host here if needed.
[pi4]
raspberrypi.local

# Examples:
#   192.168.1.50
#   raspberrypi.local ansible_user=david
